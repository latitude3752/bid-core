{
  "name": "@netacracy/bid-core",
  "version": "1.1.9",
  "private": false,
  "description": "Shared engine for the Bid product family (BidHawk, BidYard, BidPulse) -- SAM.gov client, NAICS/relevance/sync-status infra, and ZIP-distance utilities. Consumed as source via Next.js transpilePackages, not pre-built.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./data/zip-centroids.json": "./src/data/zip-centroids.json"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@supabase/supabase-js": ">=2",
    "next": ">=15",
    "react": ">=18"
  },
  "dependencies": {
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
